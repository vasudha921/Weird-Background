var red = 0;
var green = 50;
var blue=255;




function setup(){
stroke("white");
  
  var canvas = createCanvas(1200,400);
  stroke("white");
}


function draw(){
  
  background
  red.x = [220, 225, 210, 250, 240];
  green.x = [180, 166, 142, 112, 229];
  blue.y = [204, 140, 113, 172, 156];
  
  stroke("white");
  
  ellipse(World.mouseX, World.mouseY, 0);
  ellipse(220, 225, 210, 250, 240,180, 166, 142, 112, 229,204, 140, 113, 172, 156)


  // Use the map() function to do so. 



  // Remember to fill the canvas with white paint before creating the ellipse.
}